<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Próximo Spring</title>
    <link rel="stylesheet" href="/style-nextSprint.css">
</head>
<body>
<div class="container">
    <h1>Next Sprint!</h1>
    <p>Esta opción estará disponible en el próximo sprint. ¡Mantente atento!</p>
    <a href="/" class="button">Volver al inicio</a>
</div>
<script>
    // Comprobar si el modo oscuro está habilitado en el localStorage
    document.addEventListener('DOMContentLoaded', () => {
        applyDarkMode();
    });

    function applyDarkMode() {
        const isDarkMode = localStorage.getItem('darkMode') === 'true';
        if (isDarkMode) {
            document.body.classList.add('dark-mode');
        }
    }
    });
</script>
</body>
</html>
